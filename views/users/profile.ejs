<% layout('layouts/boilerplate')%>
<link rel="stylesheet" href="/stylesheets/profile.css" />

<section class="gradient-custom-2 mb-5">
  <div class="container">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-lg-9 col-xl-7">
        <div class="card">
          <div class="rounded-top text-white d-flex flex-row">
            <div class="ms-4 mt-5 d-flex flex-column">
              <img src="<%= user.avatar[0].url %>" alt="Generic placeholder image" class="img-fluid img-thumbnail" />
              <% if(currentUser && user._id.equals(currentUser._id) || currentUser && currentUser.isAdmin) { %>
              <a
                class="btn btn-outline-dark"
                id="editButton"
                type="button"
                data-mdb-ripple-color="dark"
                href="/users/<%= user._id %>/edit"
                role="button"
                >Edit Profile</a
              >
              <% } %>
            </div>
            <div class="ms-3" id="name">
              <h5><%= user.firstName + " " + user.lastName %></h5>
              <p><%= user.email%></p>
            </div>
          </div>
          <div class="p-4 text-black">
            <div class="d-flex justify-content-end text-center py-1">
              <div>
                <p class="mb-1 h5"><%= review.length %></p>
                <p class="small text-muted">Reviews</p>
              </div>
              <div class="px-2">
                <p class="mb-1 h5"><%= campground.length%></p>
                <p class="small text-muted">Campgrounds</p>
              </div>
            </div>
          </div>
          <div class="card-body p-4 text-black">
            <div class="mb-5">
              <p class="lead fw-normal mb-1">About</p>
              <div class="p-4">
                <p class="font-italic mb-1"><%= user.bio %></p>
              </div>
            </div>
            <div class="d-flex justify-content-between align-items-center mb-4">
              <p class="lead fw-normal mb-0"><%= user.username %>'s campgrounds:</p>
            </div>

            <div class="row">
              <div class="col">
                <% for (let campgrounds of campground) { %>
                <ul>
                  <a href="/campgrounds/<%= campgrounds.id %>"> <%= campgrounds.title %></a>
                </ul>
                <% } %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- <div class="col-md-3"> -->
<!-- Will add isAdmin function later for now  currentUser && currentUser.isAdmin does not run -->
<!-- <% if(currentUser && user._id.equals(currentUser._id) || currentUser && currentUser.isAdmin){ %>
  <div class="text-center">
    <form
      action="/users/<%=user._id%>?_method=DELETE"
      method="POST"
      onsubmit="return confirm('You are about to delete your profile! are you sure?') "
      id="btnDelete"
    >
      <button class="btn btn-danger userbtn">Delete Profile</button>
    </form>
  </div>
  <% } %>
</div> -->
